# Ollama Railway Deployment (Deprecated)

This guide is deprecated. RecipeNow uses the OpenAI Vision API as the primary
extractor and does not support local/self-hosted LLM deployment.

See `docs/SPEC.md` and `docs/DEPLOYMENT_CHECKLIST.md` for current setup steps.
