# Deprecated: MCP Local Design

This document is kept for history only. The repo now focuses on a persistent
agent workflow that merges the local memory kit with the Codex handoff
extension.

Use `docs/PERSISTENT_AGENT_WORKFLOW.md` as the current source of truth.
