# RecipeNow API Configuration

# API Settings
API_TITLE=RecipeNow API
API_VERSION=0.1.0

# CORS - Comma-separated list of allowed origins
# For production, add your Vercel frontend URL
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://recipenow-seven.vercel.app

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Database (if needed)
DATABASE_URL=postgresql://user:password@localhost/recipenow
