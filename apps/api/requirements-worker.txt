# Worker Service Dependencies
# Minimal dependencies for ARQ worker running LLM vision extraction
# Does NOT include PaddleOCR/PaddlePaddle (OCR fallback only used in API service)

# FastAPI (for shared models/schemas)
fastapi==0.128.0

# Database
psycopg[binary,pool]==3.3.2
sqlalchemy>=2.0.36  # Python 3.13 compatible

# Job Queue & Async (REQUIRED)
arq==0.26.3
redis==5.0.1

# Data Validation
# Use newer versions for Python 3.13 compatibility
pydantic>=2.10.0
pydantic-settings>=2.7.0

# Utilities
python-multipart==0.0.6
python-dotenv==1.0.0
httpx==0.27.0

# LLM Vision (REQUIRED for worker extraction)
anthropic==0.21.0
openai>=1.63.0
