# Worker Service Dependencies
# Minimal dependencies for ARQ worker running OCR + vision extraction

# FastAPI (for shared models/schemas)
fastapi==0.128.0

# Database
psycopg[binary,pool]==3.3.2
sqlalchemy>=2.0.36  # Python 3.13 compatible

# Job Queue & Async (REQUIRED)
arq==0.26.3
redis==5.0.1

# OCR (required for ingest_job in worker)
paddleocr[all]==3.3.2
paddlepaddle==3.0.0

# Data Validation
# Use newer versions for Python 3.13 compatibility
pydantic>=2.10.0
pydantic-settings>=2.7.0

# Utilities
python-multipart==0.0.6
python-dotenv==1.0.0
httpx==0.27.0

# LLM Vision (OpenAI vision primary)
openai>=1.63.0
